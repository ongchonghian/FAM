{
  "name": "federated-analytics-mesh",
  "version": "1.0.0",
  "description": "Federated Analytics Mesh — Privacy-Preserving 5th Corner for PEPPOL / Singapore InvoiceNow",
  "scripts": {
    "build": "cd app && npm install && npm run build"
  }
}
